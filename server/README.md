## Getting Set Up

Getting the server running on your local machine takes only a few steps:

1. clone the project
2. install its dependencies - `yarn install`
3. start the server - `yarn server.js` or `node server.js`

