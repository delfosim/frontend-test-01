import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  items: {
    marginTop: '4px',
  },
  item: {
    cursor: 'grab',
    padding: '5px',
  },
}));

export default useStyles;
