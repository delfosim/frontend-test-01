const styles = (theme) => ({
  root: {
    display: "flex",
    width: "100vw",
    height: "100vh",
    justifyContent: "center",
    background: `${theme.palette.background.default} fixed`,
    overflow: "hidden",
  },
});

export default styles;
