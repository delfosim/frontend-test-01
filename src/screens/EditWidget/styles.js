import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  save: {
    marginTop: '24px',
    width: '250px',
    maxWidth: '90%',
  },
}));

export default useStyles;
